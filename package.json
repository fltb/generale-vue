{
  "name": "generale-game-monorepo",
  "module": "index.ts",
  "type": "module",
  "private": true,
    "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --filter=./packages/* build",
    "dev": "bun run --filter=./packages/* dev"
  },

  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
